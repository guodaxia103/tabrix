/**
 * Routes module exports.
 *
 * Agent-specific routes were removed as part of the MKEP pruning
 * (see docs/PRODUCT_PRUNING_PLAN.md §1.1). This module is kept as a
 * re-export point so future route groups (memory/knowledge/policy
 * viewers in Stage 3+) can be registered from a single entry.
 */
export {};
